# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.next
*.local

# Secrets & credentials — never commit
.env
.env.*
!.env.example
!.env.sample
!.env.template
!.env.*.example

.ENV
.envrc
.direnv/

# Deploy / CI metadata (can reference project IDs or pull env previews)
.vercel/

# Package & registry auth tokens
.npmrc
!.npmrc.example

# Private keys & keystores
*.pem
*.p12
*.pfx
*.key
*.jks
*.keystore

# Common cloud / mobile secret filenames
*credentials*.json
*serviceAccount*.json
*firebase-adminsdk*.json
google-services.json
GoogleService-Info.plist

# Misc. secret-bearing tool files
.sentryclirc

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# TypeScript incremental
*.tsbuildinfo

# Local archives / large samples (not for repo)
archive (1)/
